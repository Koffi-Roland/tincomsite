{% extends '@App\\index.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <!-- MDBootstrap Datatables  -->
    <link href="{{asset('admin/css/addons/datatables.min.css" rel="stylesheet')}}">
{% endblock %}

{% block pageName %}
    <h3 class="page-header"><i class="fa fa fa-bars"></i>
        CategorieProduit
    </h3>
{% endblock %}

{% block panelHeading %}

    Liste des categorieProduits

{% endblock %}

{% block content %}
    <fieldset legend="Gestion de categorieProduit">
        <div class="panel panel-success">    
            <a class="btn btn-info" href="{{ path('categorieProduit_new') }}">Nouvel categorieProduit</a>
            <div class="table-responsive">
                <table id="dtBasicExample" class="table table-bordered table-striped table-sm" cellspacing="0" width="100%">
                    <thead class="black white-text">
                        <tr>
                            <th>Id</th>
                            <th>Libellé</th>
                            <th>Description</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for categorieProduit in categorieProduits %}
                            <tr>
                                <td>{{ categorieProduit.id }}</td>
                                <td>{{ categorieProduit.libelle|capitalize }}</td>
                                <td>{{ categorieProduit.description }}</td>
                                <td>
                                    <a class="btn btn-success" href="{{ path('categorieProduit_edit', { 'id': categorieProduit.id }) }}"><i class="fa fa-pencil"></i></a> 
                                    <a onclick="return confirm('Etes-vous sûr de vouloir supprimer ce categorieProduit')" class="btn btn-danger" href="{{ path('categorieProduit_delete', { 'id': categorieProduit.id }) }}"><i class="icon_close_alt2"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
    </fieldset>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{asset('admin/js/addons/datatables.min.js')}}"></script>
    <script>
        $(document).ready(function () {
            $('#dtBasicExample').DataTable();
            $('.dataTables_length').addClass('bs-select');
        });
    </script>
{% endblock %}