{% extends '@App\\index.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <!-- MDBootstrap Datatables  -->
    <link href="{{asset('admin/css/addons/datatables.min.css" rel="stylesheet')}}">
{% endblock %}

{% block pageName %}
    <h3 class="page-header"><i class="fa fa fa-bars"></i>
        TypeOpportunite
    </h3>
{% endblock %}

{% block panelHeading %}

    Liste des typeOpportunites

{% endblock %}

{% block content %}
    <fieldset legend="Gestion de typeOpportunite">
        <div class="panel panel-success">    
            <a class="btn btn-info" href="{{ path('typeOpportunite_new') }}">Nouveau typeOpportunite</a>
            <div class="table-responsive">
                <table id="dtBasicExample" class="table table-bordered table-striped table-sm" cellspacing="0" width="100%">
                    <thead class="black white-text">
                        <tr>
                            <th>Id</th>
                            <th>Libelle</th>
                            <th>Poste</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for typeOpportunite in typeOpportunites %}
                            <tr>
                                <td>{{ typeOpportunite.id }}</td>
                                <td>{{ typeOpportunite.libelle|capitalize }}</td>
                                <td>{{ typeOpportunite.poste.getLibelle()|capitalize }}</td>
                                <td>
                                    <a class="btn btn-success" href="{{ path('typeOpportunite_edit', { 'id': typeOpportunite.id }) }}"><i class="fa fa-pencil"></i></a> 
                                    <a onclick="return confirm('Etes-vous sûr de vouloir supprimer ce typeOpportunite')" class="btn btn-danger" href="{{ path('typeOpportunite_delete', { 'id': typeOpportunite.id }) }}"><i class="icon_close_alt2"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
    </fieldset>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{asset('admin/js/addons/datatables.min.js')}}"></script>
    <script>
        $(document).ready(function () {
            $('#dtBasicExample').DataTable();
            $('.dataTables_length').addClass('bs-select');
        });
    </script>
{% endblock %}